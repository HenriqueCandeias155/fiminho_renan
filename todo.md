# Correção do conflito npm

- [x] Diagnosticar a relação entre Vite 7 e `@builder.io/vite-plugin-jsx-loc`.
- [x] Ajustar o `package.json` e/ou a configuração do Vite para remover o peer dependency incompatível.
- [x] Validar `npm install`, `npm run check` e `npm run build`.
- [ ] Gerar um novo ZIP corrigido e entregar ao utilizador.
